<Page x:Class="TrashHandling.Pages.ImportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
	  Style="{StaticResource PageStyle}"
	  d:DesignHeight="450" d:DesignWidth="1000">
    <Grid>
		<StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" >
			<TextBlock Text="Importér fra .csv" Style="{StaticResource HeaderTextBlock}" Margin="0,0,0,20"/>
			<StackPanel Orientation="Horizontal" Width="270" HorizontalAlignment="Center">
				<!--The file name selected will be shown here-->
				<TextBlock Name="FileNameField" Width="205" Margin="0,5" Background="#FAFAFA"/>
				
				<!--Opens a file dialog to choose a .csv-file-->
				<Button Name="FileChoser" Content="Vælg fil" Margin="10,5,0,5" Width="50" Background="#FAFAFA" Click="PickFile_Click"/>
			</StackPanel>
			
			<!--DataGrid shows a preview of the data from the selected file-->
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
				<DataGrid Style="{StaticResource StandardDataGrid}" HeadersVisibility="Column" CanUserAddRows="False" CanUserReorderColumns="False" CanUserSortColumns="False" IsReadOnly="True">
					<DataGrid.Columns>
						<DataGridTextColumn Width="*" Header="Id" HeaderStyle="{StaticResource StandardGridHeaderStyle}"/>
						<DataGridTextColumn Width="*" Header="Mængde" HeaderStyle="{StaticResource StandardGridHeaderStyle}"/>
						<DataGridTextColumn Width="*" Header="Måleenhed" HeaderStyle="{StaticResource StandardGridHeaderStyle}"/>
						<DataGridTextColumn Width="*" Header="Affaldskategori" HeaderStyle="{StaticResource StandardGridHeaderStyle}"/>
						<DataGridTextColumn Width="*" Header="Affaldsbeskrivelse" HeaderStyle="{StaticResource StandardGridHeaderStyle}"/>
						<DataGridTextColumn Width="*" Header="Ansvarlig" HeaderStyle="{StaticResource StandardGridHeaderStyle}"/>
						<DataGridTextColumn Width="*" Header="VirksomhedsId" HeaderStyle="{StaticResource StandardGridHeaderStyle}"/>
						<DataGridTextColumn Width="*" Header="Dato" HeaderStyle="{StaticResource StandardGridHeaderStyle}"/>
					</DataGrid.Columns>
				</DataGrid>
			</StackPanel>
		</StackPanel>		
	</Grid>
</Page>
