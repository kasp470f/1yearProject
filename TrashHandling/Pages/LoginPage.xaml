<Page x:Class="TrashHandling.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d" 
      Style="{StaticResource PageStyle}"
      d:DesignHeight="450" d:DesignWidth="1000" 
      Title="LoginPage">

    <Grid>
		<StackPanel Style="{StaticResource HorizontalCenteredVerticalStack}">
			<TextBlock Text="Login page"/>
		</StackPanel>
			<Grid Height="200" Width="400">
				<Grid.ColumnDefinitions>
					<ColumnDefinition/>
					<ColumnDefinition/>
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition/>
					<RowDefinition/>
					<RowDefinition/>
				</Grid.RowDefinitions>
				<TextBlock Text="Navn:" Grid.Column="0" Grid.Row="0" VerticalAlignment="Center"/>
				<TextBox x:Name="UserName"  Grid.Column="1" Grid.Row="0" VerticalAlignment="Center" Background="#FAFAFA"/>
				<TextBlock Text="Vælg virksomhed:" Grid.Column="0" Grid.Row="1" VerticalAlignment="Center"/>
				<TextBox x:Name="UserCompany" Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" Background="#FAFAFA"/>
				<Button Content="Validér" Width="100" Grid.Column="1" Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Right" Background="#FAFAFA" Click="ValidateLogin_Click"/>
			</Grid>		
	</Grid>
</Page>
